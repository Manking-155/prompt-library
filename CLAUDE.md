# CLAUDE.md

Guidance for Claude Code in **prompt-library**.

## Before structural or documentation work

1. Read **`.docs/SITE.md`** — tooling map (paths, canon link).
2. Root **`README.md`** — repo layout (`prompts/agents`, `skills`, `workflows`, `docs`).

## Moboco portfolio standard

Tier **L0-T** and drift rules: **`ios-hub`** repository → `docs/moboco-portfolio-dot-docs-standard.md` (use your clone / sibling path).

## Secrets

Do not commit credentials. `inboxes/` may hold logs — treat as noisy; follow repo conventions for archiving.
